import 'package:flutter/material.dart';

class CleanupTasksScreen extends StatelessWidget {
  const CleanupTasksScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Cleanup tasks: barangay schedules and participation updates.'),
    );
  }
}
