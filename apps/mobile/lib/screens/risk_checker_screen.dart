import 'package:flutter/material.dart';

class RiskCheckerScreen extends StatelessWidget {
  const RiskCheckerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Risk checker: lifestyle inputs with rule-based score.'),
    );
  }
}
