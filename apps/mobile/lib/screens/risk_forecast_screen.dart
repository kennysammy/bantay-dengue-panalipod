import 'package:flutter/material.dart';

class RiskForecastScreen extends StatelessWidget {
  const RiskForecastScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('AI risk forecast: 3-day and 7-day trend with push notification hooks.'),
    );
  }
}
